using System.Data;
using DBtools;

namespace Academy
{
    public static class Filters
    {
        // public static DataTable GroupsDirectionsList(int direction)
        // {
        //     var connector = new Connector(); 
        //     string query = $@"SELECT  group_name,direction_name FROM dbo.Groups G
        //                         LEFT JOIN Directions D on G.direction = D.direction_id
        //                         WHERE direction = {direction};";
        //
        //     return null;
        // }
    }
}